<Window x:Class="GetHash.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GetHash"
        mc:Ignorable="d" WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Title="" Height="95" Width="500" FontSize="12">
    <Grid>
<Grid.RowDefinitions>
    <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition Height="3*"></RowDefinition>
</Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="80"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Folder's address:" VerticalAlignment="Center" Margin="20,0,0,0"/>
        <Button Grid.Row="1" Grid.Column="1" Margin="0,4,48,12" Name="CopyToClipBoardButton" ToolTip="Copy to clipboard" Click="CopyToClipBoardButton_OnClick">
            <Image Source="/Resources/CopyToClipboard2.png" />
        </Button>
        <TextBox Grid.Row="1" Margin="25,4,2,12" VerticalContentAlignment="Center" IsReadOnly="True" Name="AddressTextBox" Background="#F4F4F4"/>
    </Grid>


</Window>
